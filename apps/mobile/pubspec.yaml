name: mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.3.2+26

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^7.0.0
  flutter_svg: ^2.2.3
  dio: ^5.9.0
  http_parser: ^4.0.2
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.5.4
  provider: ^6.1.5+1
  flutter_web_auth_2: ^5.0.0
  google_sign_in: ^6.2.1
  socket_io_client: ^3.1.4
  intl: ^0.20.2
  cached_network_image: ^3.4.1
  image_picker: ^1.2.1
  url_launcher: ^6.2.5
  webview_flutter: ^4.10.0

  # Firebase & Push Notifications
  firebase_core: ^3.8.1
  firebase_messaging: ^15.2.0
  flutter_local_notifications: ^22.2.0
  google_mobile_ads: ^7.0.0
  lucide_icons: ^0.257.0
  image_cropper: ^11.0.0
  skeletonizer: ^2.1.3
  connectivity_plus: ^7.0.0
  share_plus: ^10.1.4
  like_button: ^2.0.5

  marionette_flutter: any
  easy_localization: ^3.0.8
  flutter_avif: ^3.1.0
  path_provider: ^2.1.5
  open_filex: ^4.7.0
  package_info_plus: ^9.0.1
  in_app_review: ^2.0.12
  sign_in_with_apple: ^8.0.0
  app_tracking_transparency: ^2.0.7
  app_links: ^7.0.0
  firebase_analytics: ^11.6.0
  timezone: ^0.11.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_native_splash: ^2.4.4
  flutter_launcher_icons: ^0.14.3

flutter_native_splash:
  color: "#dc143c"
  image: assets/images/logo_white_padded.png
  android_12:
    image: assets/images/logo_white_padded.png
    color: "#dc143c"
    
# Launcher icons are MANUALLY authored — see `App icon/` directory.
# DO NOT re-enable flutter_launcher_icons; it would overwrite the
# professional icon pack (Android adaptive + monochrome, iOS catalog).
# To regenerate icons: re-export from icon.kitchen and replace files in
# `App icon/`, then copy to `android/app/src/main/res/mipmap-*/` and
# `ios/Runner/Assets.xcassets/AppIcon.appiconset/`.
# flutter_launcher_icons:
#   android: "ic_launcher"
#   ios: true
#   image_path: "assets/images/app_icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/category-icons/
    - assets/translations/
    - assets/certs/
    # Latin faces google_fonts would otherwise fetch on first launch; bundled
    # so a fresh install offline (and `flutter test`) never falls back to the
    # system font. Names must stay <Family>-<Weight>.ttf (google_fonts matches
    # by suffix). Licences sit alongside.
    - assets/google_fonts/
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Nepali text. Inter (loaded by google_fonts) has no Devanagari glyphs;
  # AppFont appends this family as the per-glyph fallback everywhere.
  # Files: assets/fonts/mukta (SIL OFL 1.1, licence alongside).
  fonts:
    - family: Mukta
      fonts:
        - asset: assets/fonts/mukta/Mukta-Regular.ttf
          weight: 400
        - asset: assets/fonts/mukta/Mukta-Medium.ttf
          weight: 500
        - asset: assets/fonts/mukta/Mukta-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/mukta/Mukta-Bold.ttf
          weight: 700
        - asset: assets/fonts/mukta/Mukta-ExtraBold.ttf
          weight: 800

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
